/**
 * @file include/${project_ident}.h
 * @brief Public API for ${project_name}.
 * @details The library template starts with one exported function so build
 *          systems and tests have a concrete symbol to compile against.
 */

#ifndef ${project_macro}_H
#define ${project_macro}_H

const char *${project_ident}_name(void);

#endif /* ${project_macro}_H */
