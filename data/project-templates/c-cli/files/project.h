/**
 * @file include/${project_ident}.h
 * @brief Public declarations for ${project_name}.
 * @details The header is generated even for the small starter app so new
 *          functions have a conventional public include location.
 */

#ifndef ${project_macro}_H
#define ${project_macro}_H

#define ${project_macro}_NAME "${project_name}"

#endif /* ${project_macro}_H */
