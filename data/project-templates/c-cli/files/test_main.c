/**
 * @file tests/test_main.c
 * @brief Starter test for ${project_name}.
 * @details The first test is intentionally dependency-free so the generated
 *          project can validate its toolchain before adopting a test framework.
 */

#include <assert.h>

/**
 * @brief Run starter assertions.
 * @return Zero when assertions pass.
 */
int main(void) {
    assert(1 == 1);
    return 0;
}
