/**
 * @file main.c
 * @brief Entry point for ${project_name}.
 * @details The generated program is intentionally small so the project starts
 *          with one clear executable target before more structure is added.
 */

#include <stdio.h>

/**
 * @brief Run ${project_name}.
 * @details The first executable prints its project name. That gives the
 *          generated Makefile a visible smoke test without adding dependencies.
 * @return Zero when the program exits normally.
 */
int main(void) {
    puts("${project_name}");
    return 0;
}
